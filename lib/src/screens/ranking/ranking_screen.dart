/// Legacy ranking route path; the router redirects `/ranking` to the feed
/// shell Ranking tab and never builds a standalone ranking screen.
abstract final class RankingScreen {
  static const routePath = '/ranking';
}
